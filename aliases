# Unix
alias ll="ls -FGlahp"
alias cp="cp -r"
alias rm="trash"
alias mkdir="mkdir -p"
alias vim='nvim'

# Tmux
alias mux='tmuxinator'
alias tns="tmux new-session -s"
alias tas="tmux attach -t"
alias tds="tmux detach"
alias tls="tmux list-sessions"
alias tks="tmux kill-session -t"


# Git
alias gs="git status"
alias ga="ga ."
alias gd="git diff"
alias gdc="git diff --cached"

# ;)
alias dcss="ssh joshua@crawl.akrasiac.org" # pass: joshua
alias editlic="vim ~/Library/Application\ Support/com.ynab.YNAB4.LiveCaptive/Local\ Store/.lic"
alias y="pipx run yt-dlp"
alias dl="mkdir ~/Downloads/Music && cd ~/Downloads/Music && pipx run spotdl"
