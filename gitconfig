[user]
  name = Javon Harper
  email = javon.d.harper@gmail.com

[alias]
  conflicts = diff --name-only --diff-filter=U
  remove = push --delete origin

[color]
	ui = auto

[commit]
  template = ~/.gitmessage

[core]
  editor = vim 
	excludesfile = /Users/javon/.gitignore_global

[push]
  default = current
